{
  "name": "my-node-server",
  "version": "1.0.0",
  "description": "A basic node server that reads the api.yaml and creates routes based on it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index"
  },
  "author": "Ninad Utkrant Vadujkar",
  "license": "ISC",
  "dependencies": {
    "yaml": "^1.7.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.3"
  }
}
